const SeactionHeading = ({ heading, text }) => {
  return (
    <div className="section-heading text-center mb-4">
      <h1>{heading}</h1>
      <p>{text}</p>
    </div>
  );
};
export default SeactionHeading;
